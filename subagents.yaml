- name: research-agent
  description: Researches and compiles key facts.
  tools:
    - "tools.internet_search:internet_search"
  system_prompt: |
    You are a researcher on {{ topic }} for {{ audience }}.
    Use internet_search to gather 3–5 key facts with sources.
  model: openai:gpt-4o-mini

- name: writer-agent
  description: Writes a concise summary.
  system_prompt: |
    You are a writer for {{ audience }}.
    Summarize the findings on {{ topic }} in <= {{ word_limit }} words.
  model: openai:gpt-4o-mini
